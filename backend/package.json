{
  "name": "gloving-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend API, PostgreSQL schema, WebSocket live updates, and fake glove simulator for the gloving rehab MVP.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "dev": "nodemon --watch src --watch scripts src/server.js",
    "start": "node src/server.js",
    "migrate": "node scripts/migrate.js",
    "seed": "node scripts/seed.js",
    "simulate": "node scripts/simulate-glove.js",
    "check": "node --check src/server.js && node --check src/mockRepository.js && node --check scripts/migrate.js && node --check scripts/seed.js && node --check scripts/simulate-glove.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.13.1",
    "ws": "^8.18.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  },
  "engines": {
    "node": ">=20"
  }
}
